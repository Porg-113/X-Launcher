{
  "name": "x-client",
  "version": "1.5.0",
  "description": "Minecraft Fabric client for Windows",
  "author": "X",
  "main": "src/main.js",
  "homepage": "./",
  "scripts": {
    "start": "set ELECTRON_RUN_AS_NODE=&& electron .",
    "dev": "set ELECTRON_RUN_AS_NODE=&& electron . --dev",
    "build": "set ELECTRON_BUILDER_DISABLE_BUILD_CACHE=true&& electron-builder --win nsis portable --publish never",
    "build:installer": "set ELECTRON_BUILDER_DISABLE_BUILD_CACHE=true&& electron-builder --win nsis --publish never",
    "build:portable": "set ELECTRON_BUILDER_DISABLE_BUILD_CACHE=true&& electron-builder --win portable --publish never",
    "build:website": "node scripts/build-website.js",
    "release:github": "set ELECTRON_BUILDER_DISABLE_BUILD_CACHE=true&& electron-builder --win nsis --publish always",
    "pack": "set ELECTRON_BUILDER_DISABLE_BUILD_CACHE=true&& electron-builder --dir",
    "release:hashes": "powershell -NoProfile -File scripts/write-release-hashes.ps1",
    "test": "node --test",
    "download:fabric-mods": "node scripts/modrinth-fabric-downloader.js",
    "build:x-menu-mod": "node scripts/build-x-launcher-menu-mod.js",
    "build:instant-break-server-mod": "node scripts/build-x-instant-break-server-mod.js"
  },
  "dependencies": {
    "electron-updater": "^6.8.3",
    "node-fetch": "^2.6.7",
    "prismarine-auth": "^3.1.1"
  },
  "devDependencies": {
    "electron": "latest",
    "electron-builder": "latest"
  },
  "build": {
    "appId": "com.x.launcher.app",
    "productName": "X Client",
    "artifactName": "X-Client-${version}-${arch}.${ext}",
    "asar": true,
    "npmRebuild": false,
    "electronUpdaterCompatibility": ">=2.16",
    "publish": [
      {
        "provider": "github",
        "owner": "Porg-113",
        "repo": "X-Launcher"
      }
    ],
    "afterPack": "scripts/apply-windows-icon.js",
    "files": [
      "src/**/*",
      "index.html",
      "app.js",
      "app.parts/**/*",
      "styles.css",
      "modern-launcher.css",
      "package.json",
      "icons/**/*",
      "assets/**/*"
    ],
    "directories": {
      "buildResources": "icons",
      "output": "dist"
    },
    "win": {
      "icon": "icon.ico",
      "signAndEditExecutable": true,
      "requestedExecutionLevel": "asInvoker",
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        },
        {
          "target": "portable",
          "arch": [
            "x64"
          ]
        }
      ]
    },
    "nsis": {
      "include": "scripts/installer.nsh",
      "oneClick": false,
      "perMachine": false,
      "allowElevation": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "installerIcon": "icon.ico",
      "uninstallerIcon": "icon.ico",
      "shortcutName": "X Client"
    },
    "portable": {
      "artifactName": "X-Client-Portable-${version}.${ext}"
    }
  }
}
